# Copy to .env and fill in. NEVER commit .env.
#
# A release does NOT need this file. `.github/workflows/npm-publish.yml` reads
# the NPM_TOKEN repository secret, which is set. This is only for publishing by
# hand from a terminal, which is not the supported path.
NPM_TOKEN=
